import type { Config       fontFamily: {
        // Legacy font names (deprecated - use semantic names below)
        orbitron: ['var(--font-orbitron)', 'sans-serif'],
        outfit: ['var(--font-outfit)', 'sans-serif'],
        // Semantic font names (preferred - matches Figma tokens)
        // Note: These generate as font-{key}, so font-family-title becomes font-font-family-title
        'family-title': ['var(--font-orbitron)', 'sans-serif'],
        'family-body': ['var(--font-outfit)', 'sans-serif'],
        'family-button': ['var(--font-orbitron)', 'sans-serif'],
      },indcss';

export default {
  content: ['./src/**/*.{ts,tsx}', './docs/**/*.{md,mdx}'],
  theme: {
    extend: {
      colors: {
        bg: 'var(--bg)',
        fg: 'var(--fg)',
        muted: 'var(--muted)',
        ring: 'var(--ring)',
        border: 'var(--border)',
        brand: { 
          DEFAULT: 'var(--brand-9)', 
          fg: 'var(--brand-contrast)' 
        }
      },
      backgroundColor: {
        'glass-light': 'rgba(255, 255, 255, 0.1)',
        'glass-dark': 'rgba(0, 0, 0, 0.1)',
      },
      fontFamily: {
        // Legacy font names (deprecated - use semantic names below)
        orbitron: ['var(--font-orbitron)', 'sans-serif'],
        outfit: ['var(--font-outfit)', 'sans-serif'],
        // Semantic font names (preferred - matches Figma tokens)
        'family-title': ['var(--font-orbitron)', 'sans-serif'],
        'family-body': ['var(--font-outfit)', 'sans-serif'],
        'family-button': ['var(--font-orbitron)', 'sans-serif'],
        // CRITICAL: Add hyphenated versions for correct Tailwind class generation
        'font-family-title': ['var(--font-orbitron)', 'sans-serif'],
        'font-family-body': ['var(--font-outfit)', 'sans-serif'],
        'font-family-button': ['var(--font-orbitron)', 'sans-serif'],
      },
      fontSize: {
        'hero-title': 'var(--text-hero-title)',
        'hero-desc': 'var(--text-hero-desc)',
        'section-title': 'var(--text-section-title)',
        'section-desc': 'var(--text-section-desc)',
        'card-title': 'var(--text-card-title)',
        'card-subtitle': 'var(--text-card-subtitle)',
        'card-price': 'var(--text-card-price)',
        'card-title-nft': 'var(--text-card-title-nft)',
        'card-author': 'var(--text-card-author)',
        'card-badge': 'var(--text-card-badge)',
        'card-price-nft': 'var(--text-card-price-nft)',
      },
      fontWeight: {
        'regular': 'var(--font-weight-regular)',
        'medium': 'var(--font-weight-medium)',
        'bold': 'var(--font-weight-bold)',
        'black': 'var(--font-weight-black)',
      },
      borderRadius: {
        md: 'var(--radius-md)',
        lg: 'var(--radius-lg)',
        card: 'var(--radius-card)',
        'card-sm': 'var(--radius-card-sm)',
      },
      backdropBlur: {
        'glass': 'var(--glass-blur)', // 100px - main glass effect (Figma 200÷2)
        'glass-blur': 'var(--glass-blur)', // Alias for 'glass' (semantic naming)
        'glass-sm': 'var(--glass-blur-sm)', // 4px - small blur
        'countdown': 'var(--backdrop-blur-3-723)', // 3.723px - countdown badge
        's': 'var(--blur-s)',
      },
      boxShadow: {
        'card': 'var(--shadow-card)',
        'card-hover': 'var(--shadow-card-hover)',
      },
      spacing: {
        'container': 'var(--container-padding)',
        '0.5': 'var(--spacing-0-5)',
        'negative-3': 'var(--spacing-negative-3)',
        'negative-6': 'var(--spacing-negative-6)',
      },
      maxWidth: {
        'container': 'var(--container-max)',
      },
      height: {
        'header': 'var(--header-h)',
        'footer': 'var(--footer-h)',
      },
      minHeight: {
        'hero': 'var(--hero-h)',
      }
    }
  },
  plugins: []
} satisfies Config;
